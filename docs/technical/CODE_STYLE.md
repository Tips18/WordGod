# 编码规范

## 通用规则

- TypeScript 全仓启用严格模式。
- 所有函数都必须带总结式注释。
- 命名优先表达业务含义，避免 `data`、`info` 这类空泛名称。
- 前后端公共结构统一放在 `packages/contracts/`，禁止重复手写。

## 测试规则

- 新增行为先写失败测试，再写生产代码。
- 后端使用 Jest，前端使用 Vitest 和 Testing Library。
- 涉及主链路的功能必须补充端到端测试。

